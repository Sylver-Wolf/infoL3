{% extends 'base.html.twig' %}

{% block title %}Test index{% endblock %}

{% block body %}
    <h1>Test index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for test in tests %}
            <tr>
                <td>{{ test.id }}</td>
                <td>
                    <a href="{{ path('test_controler_show', {'id': test.id}) }}">show</a>
                    <a href="{{ path('test_controler_edit', {'id': test.id}) }}">edit</a>
                    <a href="{{ path('test_controler_jsp', {'id': test.id}) }}">jsp</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('test_controler_new') }}">Create new</a>
{% endblock %}
